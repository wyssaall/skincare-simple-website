
import express from "express";

const router = express.Router();

// get all products

router.get('/products', );

// get single product
router.get('/prodects/:id',);

//create a product
router.post('/products',);

//update a product
router.put('/products/:id',);

//deletes a product
router.delete('/products/:id',);

export default router;
